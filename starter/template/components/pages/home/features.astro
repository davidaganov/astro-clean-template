<section
  class="features"
  id="features"
  aria-labelledby="features-title"
>
  <div class="container">
    <h2
      id="features-title"
      class="features__heading"
    >
      Goals & Philosophy
    </h2>
    <ul class="features__grid">
      <li class="features__card">
        <span class="features__icon">🧩</span>
        <h3 class="features__title">Authoring Freedom</h3>
        <p class="features__text">
          Components and shared layouts without duplicated markup, shipped as predictable, readable
          static code assets.
        </p>
      </li>
      <li class="features__card">
        <span class="features__icon">📦</span>
        <h3 class="features__title">Unified CSS Pipeline</h3>
        <p class="features__text">
          Aggregates to one simple <code>main.css</code> in <code>dist/</code> using native CSS nesting
          and layout wrappers.
        </p>
      </li>
      <li class="features__card">
        <span class="features__icon">🚀</span>
        <h3 class="features__title">Script Bundling Modes</h3>
        <p class="features__text">
          Default maintains exact files for local templates; <code>build:prod</code> bundles resolving
          imports completely.
        </p>
      </li>
      <li class="features__card">
        <span class="features__icon">🖼️</span>
        <h3 class="features__title">Optimized Asset Copies</h3>
        <p class="features__text">
          Uses <code>vite-plugin-static-copy</code> to manage non-bundled asset delivery for standard
          layout paths.
        </p>
      </li>
    </ul>
  </div>
</section>

<style is:global>
  .features {
    padding: clamp(64px, 10vw, 96px) 0;
    position: relative;
    z-index: 2;
  }

  .features__heading {
    margin: 0 0 40px;
    font-size: 1.5rem;
    font-weight: 700;
    text-align: center;
    letter-spacing: -0.02em;
    color: var(--color-text);
  }

  .features__grid {
    list-style: none;
    display: grid;
    gap: 24px;
    margin: 0;
    padding: 0;

    @media (min-width: 640px) {
      grid-template-columns: repeat(2, 1fr);
    }

    @media (min-width: 1024px) {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  .features__card {
    position: relative;
    padding: 24px;
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-sm);
    backdrop-filter: blur(8px);
    transition: all 0.3s ease;

    &:hover {
      background: rgba(255, 255, 255, 0.04);
      border-color: rgba(255, 255, 255, 0.12);
      transform: translateY(-2px);
      box-shadow: 0 8px 24px rgba(184, 126, 239, 0.05);
    }
  }

  .features__icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    margin-bottom: 16px;
    font-size: 20px;
    background: rgba(184, 126, 239, 0.1);
    border-radius: 8px;
    color: var(--color-accent);
  }

  .features__title {
    margin: 0 0 10px;
    font-size: 16px;
    font-weight: 600;
    letter-spacing: -0.01em;
    color: var(--color-text);
  }

  .features__text {
    margin: 0;
    font-size: 14px;
    line-height: 1.6;
    color: var(--color-muted);

    & code {
      font-family: ui-monospace, monospace;
      font-size: 0.88em;
      padding: 0.12em 0.3em;
      border-radius: 4px;
      background: rgba(255, 255, 255, 0.05);
      color: #ffffff;
    }
  }
</style>
